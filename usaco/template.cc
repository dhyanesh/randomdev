/*
ID:  dhyanes1
LANG: C++
PROG: template
*/
#include <cstdio>
#include <algorithm>
#include <map>
#include <queue>

using namespace std;

int main(void)
{
  FILE *fin,*fout;
  fin=fopen("template.in","r");
  fout = fopen("template.out","w");
  fclose(fout);
  fclose(fin);
  return 0;
}
